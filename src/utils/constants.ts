export enum ROUTES {
    HOMEPAGE = "/",
    LOGIN = "/login",
    RULES = "/rules",
    SIGNUP = "/signup",
    SINGLE_PLAYER = "/single-player",
    STATS_LIFETIME = "/stats/all-time",
    STATS_TODAY = "/stats/today",
}

export const APP_NAME = "Casino Land";
